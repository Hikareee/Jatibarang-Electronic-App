{
  "name": "ibasa-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for IBASA (builds web app from parent folder)",
  "scripts": {
    "build:web": "cd .. && VITE_APP_BASE=./ npm run build",
    "sync": "npm run build:web && npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npm run sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
